# ==============================================================================
# Procfile - Heroku Process Configuration
# ==============================================================================
# Defines the command Heroku should run to start the bot.
# 
# 'worker' type is used for bots (not web servers).
# The 'start' script launches the bot with proper configuration.
# 
# Note: This is used when deploying without Docker (buildpack deployment).
# For Docker deployment, heroku.yml takes precedence.
# ==============================================================================

worker: bash start
